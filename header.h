#include <stdio.h>

void printBorder();
void printTitle();
void menuSelect(int *menuChoicePtr);
// void executeChoice(int menuChoice, int* menuChoicePtr, FILE* playerList);
void startGame(int* menuChoicePtr, FILE* playerList);